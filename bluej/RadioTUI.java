
/**
 * Write a description of class RadioGUI here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */

/**
 * A user should be able to do the following:
 * • Create a Medium (for instance a CD) and add the necessary information about this media.
 * • Add one or more tracks on the media created (like the music tracks on the CD), and add
 *   information about the tracks
 * • Change information already registered about a media
 * • Change information already registered about an audio track
 * • Search the archive by archive-number (note that not all medias have archive numbers...)
 * • Search the audio tracks by title of (or part of the title of) the audio track
 * • Search the audio tracks by artist (note that not all the tracks have an artist)
 * • List the content of the archive in different ways (all music tracks (including reference to the
 *   media), all CDs (including the tracks on the CD) etc)
 */
public class RadioTUI
{
    //HEI

    /**
     * Starting with a Text UI for testing before making a GUI.
     */
    public RadioTUI()
    {
        TUI();
    }
    
    /**
     * 
     */
    public void TUI()
    {
        //System.out.print som fææn
    }

}
